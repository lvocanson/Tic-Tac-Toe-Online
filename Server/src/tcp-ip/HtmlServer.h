#pragma once
#include <WinSock2.h>

class HtmlServer final
{
public:
    HtmlServer() = default;
    ~HtmlServer() = default;
    HtmlServer(const HtmlServer&) = delete;
    HtmlServer& operator=(const HtmlServer&) = delete;

private:
};
